[project]
name = "opencode-wiki"
version = "0.1.0"
description = "Persistent markdown knowledge base compiled from OpenCode conversations."
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
